Download and Install Git:
https://git-scm.com/download/win

After installation add user name and email into git global configuration

Open GitBash command prompt:

$ git config --global user.name "Md. Abu Bakar Siddique"
$ git config --global user.email "abms.siddique@gmail.com"

View Configuration:

$ git config --list
$ git config user.name
$ git config user.email

/*--------------------------------------------------------------------*/
Tutorial from following link:
https://www.youtube.com/watch?v=SVaV6beHZE8&list=PLoR56CteKZnC0lBlHdnVnq0J3yDhgbi9w&index=3

Go to Project Folder using bash:
Hard drive change command : cd /d/Tutorial/Bootstrap4_Tutorial
$ git init

Finally added local git repository into your project folder.

Add new file into that folder and git will track any changes on the folder.
View changes tracking status command:
$ git status

Added that file under change tracking in git following command
$ git add index.html

Commit command:
$ git commit -m "Added index.html file"

View Status command: 
$ git status

After modify anyghing we can see changes on that file using this command:
$ git diff

After modification need to commit using following command:
$ git commit -m "added jumbotron div tag" index.html

All files commit in one command:
$ git commit -am "Index and WebsiteDemo file commit."

$ git commit -m "Both file are changed" index.html websitedemo.html

View changed log command:
$ git log

How to see specific commited changed using starting 5 hash key characters:
$ git show f5da6c9

How to go previous commited stage:
$ git checkout f5da6c9

Revert last commited stage: 
$ git checkout master

How to go previous commited stage from a specific file:
$ git checkout f5da6c9 services.html

Revert last commited stage from a specific file:
$ git checkout master -f

Reset commit 2 types: 1. Soft 2. Hard
Note: Do not commit reset after push to repository.


$ git reset --soft f5da6c9 
(It will go to previous commited stage but changes text exists on the file)

$ git reset --hard f5da6c9 
(It will go to previous commited stage but changes text exists on the file)

View all commited RefLog:
$git reflog

Revert from hard reset
$ git reset HEAD{8}
$ git reset --hard

Stash command use for temporary save to git:
$ git stash

Revert stash data command:
$ git stash pop

Stash List show command:
$ git stash list

Stash list clean command:
$ git stash clean

Untracked files remove command

To show untracked files to be deleted:
$ git clean -f -n 

Clean command:
$ git clean -f

/* ---------------------------------------------------------- */
Follwing command use to connect remote repository like 
GitHub, BitBucket, GitLab, etc. We can also add multiple repository 
in a project. 

New procject add GitHub repository command:
Using SSH:
$ git clone git@github.com:smilegse/Bootstrap4_Tutorial.git
Using HTTPS:
$ git clone https://github.com/smilegse/Bootstrap4_Tutorial.git

Existing project add GitHub repository command:
Using SSH:
$ git remote add origin git@github.com:smilegse/Bootstrap4_Tutorial.git
Using HTTPS:
$ git remote add origin https://github.com/smilegse/Bootstrap4_Tutorial.git

Show remote repository command:
$ git remote show

Show remote repository url command:
$ git remote show origin

/*------------------------------------------------------------------------------------------- */ 
Problem solved using SSH authentication: git@github.com: Permission denied (publickey).

https://help.github.com/en/github/authenticating-to-github/error-permission-denied-publickey

Generating a new SSH key and adding it to the ssh-agent:
https://help.github.com/en/github/authenticating-to-github/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent

Adding a new SSH key to your GitHub account:
https://help.github.com/en/github/authenticating-to-github/adding-a-new-ssh-key-to-your-github-account

Auto-launching ssh-agent on Git for Windows:
https://help.github.com/en/github/authenticating-to-github/working-with-ssh-key-passphrases

/* -----------------------------------------------------------------------------------------------*/

Git SSH passphrases/Password : silicsa%2010

Set defalut SSH passphrases command:
$ vim ~/.profile
press key i[insert]
copy following script : 

env=~/.ssh/agent.env

agent_load_env () { test -f "$env" && . "$env" >| /dev/null ; }

agent_start () {
    (umask 077; ssh-agent >| "$env")
    . "$env" >| /dev/null ; }

agent_load_env

# agent_run_state: 0=agent running w/ key; 1=agent w/o key; 2= agent not running
agent_run_state=$(ssh-add -l >| /dev/null 2>&1; echo $?)

if [ ! "$SSH_AUTH_SOCK" ] || [ $agent_run_state = 2 ]; then
    agent_start
    ssh-add
elif [ "$SSH_AUTH_SOCK" ] && [ $agent_run_state = 1 ]; then
    ssh-add
fi

unset env

/* ------------------------------------------------------------------ */

Press ESC Key :wq command to save file. 


Remote repository push command:
$ git push origin master
$ git push origin master --force

Remote repository pull command:
$ git pull origin master

How to create command alias :
$ alias gc="git commit"





















